# Ralph Progress Log
Started: Ship or Skip project
---

## Codebase Patterns
- Next.js 16+ uses Tailwind v4 with `@theme inline` in globals.css for custom colors
- Use `--color-*` CSS variables in @theme block for Tailwind utility class support
- Inter font loaded via `next/font/google` with `variable: "--font-inter"`
- Dark theme enforced via `className="dark"` on html element
- Supabase clients: `client.ts` for browser, `server.ts` for server-side with service role key
- TypeScript types in `lib/supabase/types.ts` - import `Database` for createClient generic
- **CRITICAL:** Supabase Database type MUST include `Relationships: []` in each table and `CompositeTypes` in schema for proper type inference
- API routes use `.limit(1).returns<Type[]>()` instead of `.single()` for single record fetches to avoid `never` type issues
- For Supabase write operations, use explicitly typed variables (e.g., `const insert: VoteInsert = {...}`)
- SQL migrations in `supabase/migrations/` numbered 001_, 002_, etc.
- Components organized in `src/components/` by feature (e.g., `voting/`, `submit/`, `layout/`)
- Session management: `lib/session/manager.ts` with `getOrCreateSessionId()` for server components
---

## 2026-01-15 - US-001
- What was implemented: Initialized Next.js project with Tailwind CSS and Ship or Skip theme
- Files changed:
  - ship-or-skip/src/app/globals.css (custom color theme)
  - ship-or-skip/src/app/layout.tsx (Inter font, dark theme)
  - ship-or-skip/src/app/page.tsx (simple placeholder)
- **Learnings for future iterations:**
  - Next.js 16+ create-next-app uses Tailwind v4 with new @theme syntax
  - Custom colors are defined in globals.css using CSS variables and @theme inline block
  - Build and typecheck run from ship-or-skip directory with `npm run build`
  - Project structure: src/app/ for App Router pages
---

## 2026-01-15 - US-002
- What was implemented: Set up Supabase database connection and SQL migrations
- Files changed:
  - ship-or-skip/src/lib/supabase/client.ts (browser Supabase client)
  - ship-or-skip/src/lib/supabase/server.ts (server Supabase client)
  - ship-or-skip/src/lib/supabase/types.ts (TypeScript types for all tables)
  - ship-or-skip/supabase/migrations/001_create_ideas_table.sql
  - ship-or-skip/supabase/migrations/002_create_votes_table.sql
  - ship-or-skip/supabase/migrations/003_create_sessions_table.sql
  - ship-or-skip/package.json (added @supabase/supabase-js dependency)
- **Learnings for future iterations:**
  - Use `createClient<Database>` generic to get typed Supabase queries
  - Server client uses SUPABASE_SERVICE_ROLE_KEY for admin access
  - Browser client uses NEXT_PUBLIC_SUPABASE_ANON_KEY for public access
  - Types file exports convenience types: Idea, IdeaInsert, Vote, VoteInsert, etc.
  - SQL migrations should use IF NOT EXISTS for idempotency
---

## 2026-01-15 - US-003
- What was implemented: Built IdeaCard component for displaying startup ideas
- Files changed:
  - ship-or-skip/src/components/voting/idea-card.tsx (new component)
- **Learnings for future iterations:**
  - Components go in `src/components/` organized by feature (e.g., `voting/`, `submit/`)
  - Use `Pick<Idea, "hero" | "subtitle">` to type props with only needed fields from Idea type
  - Tailwind custom colors work via `bg-surface`, `text-foreground` etc. from @theme inline
  - Use `rounded-2xl` for 16px border-radius in Tailwind
  - Use `py-12` for 48px vertical padding (12 * 4px = 48px)
---

## 2026-01-15 - US-004
- What was implemented: Built VoteButtons component with Ship and Skip buttons
- Files changed:
  - ship-or-skip/src/components/voting/vote-buttons.tsx (new component)
- **Learnings for future iterations:**
  - Export `VoteType = "ship" | "skip"` type for consistent vote type handling
  - Use `"use client"` directive for components with onClick handlers
  - Hover glow: `hover:shadow-[0_0_20px_rgba(R,G,B,0.5)]` with color-matched RGBA
  - Hover scale: `hover:scale-[1.02]`, active press: `active:scale-[0.98]`
  - Use `min-h-16` (64px) for large tap targets
  - `flex-col sm:flex-row` for responsive stacking (full width mobile, side-by-side desktop)
  - Disabled state: `disabled:opacity-50 disabled:cursor-not-allowed` plus reset hover effects
---

## 2026-01-15 - US-005
- What was implemented: Built RevealOverlay component for showing voting results
- Files changed:
  - ship-or-skip/src/components/voting/reveal-overlay.tsx (new component)
- **Learnings for future iterations:**
  - Import `VoteType` from vote-buttons.tsx for consistent vote type handling
  - Use `bg-background/50` for subtle nested background contrast
  - Conditional rendering with `{condition && <element>}` for optional sections
  - Use `.toLocaleString()` for formatted vote counts (e.g., "1,234")
  - Border button style: `border border-foreground/20` with hover `border-foreground/40`
---

## 2026-01-15 - US-006
- What was implemented: Created session management utility for cookie-based anonymous user tracking
- Files changed:
  - ship-or-skip/src/lib/session/manager.ts (new file)
- **Learnings for future iterations:**
  - Use `cookies()` from `next/headers` for server-side cookie access (requires await in Next.js 16+)
  - Use `crypto.randomUUID()` for generating UUID v4 session IDs
  - Session cookie settings: httpOnly, secure in production, sameSite: "lax", 1 year maxAge
  - Export both `getOrCreateSessionId()` (creates if missing) and `getSessionId()` (returns null if missing)
  - Cookie name: "ship-or-skip-session" for this project
---

## 2026-01-15 - US-007
- What was implemented: Created GET /api/ideas/next endpoint for fetching next unvoted idea
- Files changed:
  - ship-or-skip/src/app/api/ideas/next/route.ts (new file)
- **Learnings for future iterations:**
  - Supabase typed client with partial selects may return `never` types - use `.returns<Type[]>()` to fix
  - Import convenience types (Vote, Idea) from types.ts for explicit return typing
  - Weighted random selection: use inverse weight (1/(total_votes+1)) to favor low-vote ideas
  - Handle conditional `.not()` filter by using ternary with two separate query chains
  - API routes in Next.js App Router: export named `GET`/`POST` functions from route.ts
---

## 2026-01-15 - US-008
- What was implemented: Created POST /api/ideas/vote endpoint for recording votes and returning results
- Files changed:
  - ship-or-skip/src/app/api/ideas/vote/route.ts (new file)
  - ship-or-skip/src/lib/supabase/types.ts (added Relationships: [] and CompositeTypes to Database type)
- **Learnings for future iterations:**
  - Supabase v2.90+ requires `Relationships: []` and `CompositeTypes` in Database type definition for proper type inference
  - Use `.limit(1).returns<Type[]>()` instead of `.single()` to avoid type inference issues with `never` types
  - For write operations (insert/update), use explicit type annotations like `VoteInsert`, `IdeaUpdate` etc.
  - Create separate supabase client for write operations if type chain gets confused after `.returns<>()` calls
  - Calculate user_agreed_with_crowd based on updated stats (after the vote is counted) to avoid off-by-one logic issues
  - Session stats tracking: create new session on first vote, update existing session on subsequent votes
---

## 2026-01-15 - US-009
- What was implemented: Built voting page with full voting flow
- Files changed:
  - ship-or-skip/src/app/vote/page.tsx (server component wrapper)
  - ship-or-skip/src/app/vote/voting-client.tsx (client component with voting logic)
- **Learnings for future iterations:**
  - Split page into server component (page.tsx) and client component for session handling
  - Server component fetches session ID via `getOrCreateSessionId()` and passes to client
  - Client component manages state: loading, voting, voteResult, userVote, noMoreIdeas
  - Use `useCallback` for fetch functions to avoid infinite re-renders with `useEffect`
  - API returns 404 when no more ideas - handle this to show "all done" state
  - Use `navigator.share()` for native sharing with fallback to `clipboard.writeText()`
  - Loading skeleton with `animate-pulse` for better UX while fetching
---

## 2026-01-15 - US-010
- What was implemented: Built landing page with hook stat and call to action
- Files changed:
  - ship-or-skip/src/app/page.tsx (updated with full landing page)
- **Learnings for future iterations:**
  - Use `next/link` Link component for client-side navigation
  - Use `&apos;` entity for single quotes in JSX to avoid eslint errors
  - Hook stats in colored highlights: `<span className="text-skip font-bold">73%</span>` draws attention
  - Same hover/active animation pattern as vote buttons: `hover:scale-[1.02] active:scale-[0.98]`
  - Simple "How it works" section with `space-y-1` and muted text helps explain app
---
